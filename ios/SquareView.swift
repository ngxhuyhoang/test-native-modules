import SwiftUI

struct SquareView: View {
    var body: some View {
        VStack {
            Text("Haha")
                .frame(width: 100, height: 100)
                .background(.red)
        }
    }
}
